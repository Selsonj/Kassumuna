rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permite leitura e escrita em qualquer subpasta de 'orders' se autenticado
    match /orders/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
